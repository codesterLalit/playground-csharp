namespace Play.cleanarch.Domain;

// TODO: a record Book with Id (int), Title (string), AuthorId (int)
// TODO: also add an extra init-only property: public Author? Author { get; init; }
//       This navigation property isn't used until Exercise 13 (Include) — declaring it now keeps the
//       entity model stable across Exercises 11-15 instead of changing shape partway through.
