using Play.cleanarch.Domain;

namespace Play.cleanarch.Application;

public class ProductCatalogService
{
    // TODO: constructor takes IProductRepository
    // TODO: AddProduct(int id, string name, decimal price) — builds a Product, adds via repository
    // TODO: GetProduct(int id) -> Product?
}